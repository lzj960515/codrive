# 账单导出
产品范围为后端账单导出服务，没有 UI。共享 money 模块返回 amountCents，两个调用包 api 和 batch 分别读取该字段。CI逐包独立运行；它们由不同发布单元维护，不能在同一窗口全部切换。
已确认迁移决定：money 增加同值字段 amountMinor，两个调用包分批改用它，已验证两包均退出旧字段后再移除 amountCents。过渡期新旧字段值必须一致。目的只是明确金额最小单位命名，不改金额计算、币种或取整规则。
现在准备新增迁移工作。同时有普通 backlog 任务“修正日志错字”，仅把日志文本 lenght 改为 length，行为和接口不变。
